# The program parses the first character as a number and then compares the following characters against the square of the first. If they are the same a success message is shown. The following code shows all of the correct keys.

# Have a great day :)
# github.com/johnnnathan

for i in range(1, 10):
    print("" + str(i) + "" + str(i**2))
